# Manual Test Case: Verify HTTP GET call to jsonplaceholder

**Test Steps:**
1. Open a web browser.
2. Navigate to: `https://jsonplaceholder.typicode.com/posts/1`
3. Observe the JSON response displayed.

**Expected Result:**
- The response contains a JSON object with the property `"id": 1`.
- The HTTP status code is 200 (OK).
